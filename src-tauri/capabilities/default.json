{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Post",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "stronghold:default",
    "updater:default",
    "store:default",
    "process:default",
    "shell:allow-open"
  ]
}
